#include "types.h"
#include "stat.h"
#include "user.h"
#include "fcntl.h"
// #include "stdio.h"

int main(int argc, char *argv[])
{
    // int num = atoi(argv[1]);
    // incNum(num);
    // printf(1 ,"Orginal number is %d\n",num);
    // getprocs();
    // printstack();
    // void (*fcn)(void *), *arg, *stack;

    // argptr(0, (void *)&fcn, sizeof(void (*)(void *)));
    // argptr(1, (void *)&arg, sizeof(void *));
    // argptr(2, (void *)&stack, sizeof(void *));
    // thread_create(fcn, arg);
    exit();
}